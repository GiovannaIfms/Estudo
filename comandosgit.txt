git init
git config --global user.email "seu email"
git config --global user.name "Vitor Hugo"
git remote add origin "repositorio"
git add .
git commit -m "Mensagem/Comentário"
giit push origin master

(para colar algo= shift + insert)